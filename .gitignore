.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
dist/
build/
*.egg-info/
*.sqlite
*.sqlite-wal
*.sqlite-shm
.env

# The compose bind-mount target. A case lives here; it is not the project's.
/case/

# Case data and HTTP cache from local runs. Never source.
.chainscope/

# Attribution datasets are fetched, never bundled. A committed copy silently
# becomes "the version this package shipped with" --- and for a list whose
# entries accuse addresses, a stale snapshot is worse than no snapshot.
# See docs/data-sources.md for where each comes from.
data/labels/
